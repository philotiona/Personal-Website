import type { ReactNode } from "react";

export default function About(): ReactNode {
    return(
        <section className="bg-[var(--color-ultramarine)] w-full h-screen flex flex-row flex-wrap">
            About
        </section>
    )
}
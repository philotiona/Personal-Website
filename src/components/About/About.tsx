import type { ReactNode } from "react";

export default function About(): ReactNode {
    return(
        <section className="bg-[var(--color-whitish)] w-full h-screen flex flex-row flex-wrap text-8xl text-[var(--color-ultramarine)]">
            About
        </section>
    )
}